node_modules/
*.vsix
package-lock.json
.atl/

# Archivo de trabajo local para gh release --notes-file (nunca se versiona)
RELEASE_NOTES.md

# Seguimiento ODD: caché local de tasks (el respaldo persistente vive en Engram)
odd/
