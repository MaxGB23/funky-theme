# Feature: v3.1rc1 — Inputs elevados, PeekView, indent guides y accesibilidad HC

## Objective
Propagar al source of truth la propuesta probada visualmente (`.atl/propuesta-v3.1rc1.md`):
- Inputs/checkbox/dropdown elevados (`bgElevated`) en las 5 variantes.
- Indent guides de editor nuevos + guías de árbol con accent.
- PeekView nuevo completo.
- Fix accesibilidad HC: gutters ignored/untracked suben a `uiInactive` (solo HC).
- HC: eliminar deletes de `button.secondary*` (se hereda base), `list.activeSelectionBackground` #8c8eff33, indent guides HC.
- Corregir comment desactualizado en `palette.comments` (los comments reales usan literal `#d8d8d8f1`).
- Update README (filosofía fondo + estado accesibilidad HC) según propuesta.

## Why
Propuesta v3.1rc1 testada visualmente por el usuario (criterio de aceptación del repo: aprobación visual primero, propagación después).

## Scope
Solo source of truth y artefactos compilados. NO tocar `.atl/` ni `themes/*.json` a mano.
Gutter fix = SOLO variante HC (decisión de alcance del usuario). Los demás cambios aplican a las 5 variantes (Darker con overrides).

## Constraints
- 5 variantes: Dark, Italic, Mix (base compartido); Darker (overrides build.js); HC (overrides build.js).
- Tokens nuevos si repiten ≥2 usos: `guideAccent: "#8c8eff73"` (2 usos: tree.indentGuidesStroke + tree.tableColumnsBorder) y `guideMid: "#625e74"` (2 usos: editorIndentGuide.activeBackground1 base + editorIndentGuide.background1 HC).
- Hex en minúsculas siempre (normalizar #2E3250 → #2e3250, #3A374A → #3a374a, etc.).
- Valores de un solo uso → literales (p.ej. `#464254`, `#2e3250`, `#8c8eff1e`, `#ffffff90`).
- Reusar tokens existentes: `bgDeep` (#211e2b), `bgElevated` (#2e2a3a), `uiAccentStrong` (#8c8effd2), `searchBackground` (#5f569580), `uiInactive` (#8b8f9e).
- Respetar zonas semánticas de `theme-config.js`.

## Tasks
- [x] T1: Palette — añadir `guideAccent` y `guideMid`; corregir comment de `palette.comments`.
- [x] T2: Colors base — editorIndentGuide; tree.* → guideAccent; bloque PeekView; inputs/checkbox/dropdown → bgElevated; inputValidation; list.activeSelectionBackground → #8c8eff1e.
- [x] T3: build.js Darker — map darkerBackgrounds: `'#464254': '#3a374a'` y `[palette.guideMid]: '#544f64'` (excepciones indent guides).
- [x] T4: build.js HC — editorIndentGuide.*, tree.tableColumnsBorder, peekView.border, quitar deletes button.secondary*, list.activeSelectionBackground, override gutters → uiInactive.
- [x] T5: Build `node scripts/build.js` y verificar JSON 5 variantes (spot checks).
- [x] T6: Docs — README filosofía + estado HC; `docs/how-to-modify-theme.md` (2 tokens nuevos en tabla paleta).

## Verification evidence
- `node scripts/build.js` ✅ (build ok, 5 variantes)
- Spot check JSONs (compilados):
  - HC: `editorIndentGuide.background1` #625e74; `activeBackground1` #8c8effd2; gutters ignored/untracked `#8b8f9e`; `peekView.border` #8c8effd2; `list.activeSelectionBackground` #8c8eff33; `button.secondaryBackground` #313244 (ya no se borra); `input.border` #8c8effd2.
  - Darker: indent guides #3a374a/#544f64; `input.background` #201d2a; `peekViewEditor.background` #121018.
  - Dark: indent guides #464254/#625e74; `tree.indentGuidesStroke` #8c8eff73; `input.background` #2e2a3a; `peekViewEditor.background` #211e2b; `list.activeSelectionBackground` #8c8eff1e; gutters base siguen #606685 (solo HC cambia).
- Nota: `palette.controlBorder` quedó SIN USOS (0 referencias) — comment actualizado como tal (mismo patrón que fgMuted/purpleSoft).

## Work-unit commit
- Fecha: 2026-09-23 — commit de producto (se registrará el hash aquí tras el commit).
- 226+ insertions / 65 deletions ≈ 291 líneas — bajo presupuesto 400, single PR candidato.
- Build: `node scripts/build.js` ✅ 5 variantes.